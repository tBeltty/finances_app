# Tests

This directory contains the testing infrastructure for the project.

## Structure
- `client/`: Frontend tests (Unit, Integration, E2E)
- `server/`: Backend tests (Unit, Integration)
- `shared/`: Tests for shared utilities

## Running Tests
*Instructions to be added when test runner is configured.*
