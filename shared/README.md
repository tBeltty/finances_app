# Shared Utilities

This directory contains code shared between the client and server applications.

## Contents
- `types/`: Shared TypeScript interfaces/types (if applicable)
- `constants/`: Shared constants
- `utils/`: Shared utility functions
- `validation/`: Shared validation schemas (e.g., Zod/Yup)

## Usage
Import these modules into both client and server projects to ensure consistency.
